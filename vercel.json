{
  "framework": "vite",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @sentinel/dashboard build",
  "outputDirectory": "apps/dashboard/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
